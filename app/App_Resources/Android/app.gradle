// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nativescript.sitepower.io.mobile"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
